// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Genre {
  key              String   @id @map("_id") @db.ObjectId
  value            String
  movieIDs         String[] @db.ObjectId
  movies            Movie[]  @relation(fields: [movieIDs], references: [id])
}

model Star {
  id               String   @id @map("_id") @db.ObjectId
  name             String  
  movieIDs         String[] @db.ObjectId
  movies           Movie[]  @relation(fields: [movieIDs], references: [id])
}

model User {
  id               String   @id @map("_id") 
  movieIDs         String[] @db.ObjectId
  movies           Movie[]  @relation(fields: [movieIDs], references: [id])
}

model Movie {
  id               String   @id @default(auto()) @map("_id") @db.ObjectId
  imDbId           String?
  image            String?
  title            String
  description      String?
  runtimeStr       String?
  genres           String?
  genreIDs         String[] @db.ObjectId
  genreList        Genre[]  @relation(fields: [genreIDs], references: [key])
  contentRating    String?
  imDbRating       String?
  imDbRatingVotes  String?
  metacriticRating String?
  plot             String?
  stars            String?
  starIDs          String[] @db.ObjectId
  starList         Star[]   @relation(fields: [starIDs], references: [id])
  userIDs          String[]
  users            User[]   @relation(fields: [userIDs], references: [id])
}